{
    "presets": [
        "@babel/preset-env",
        "@babel/preset-typescript",
        ["@babel/preset-react", {
            "runtime": "automatic" // чтобы не указывать import React>17 в каждом файле
        }]
    ],
    "plugins": [
        "i18next-extract"
    ]
}